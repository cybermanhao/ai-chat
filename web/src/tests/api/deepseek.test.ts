// web/src/tests/api/deepseek.test.ts
import { describe, it, expect } from 'vitest';

describe('web 独有 API 测试示例', () => {
  it('should work for web only', () => {
    expect(1 + 1).toBe(2);
  });
});
